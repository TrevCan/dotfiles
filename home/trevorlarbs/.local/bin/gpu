#!/usr/bin/sh

if [ "$1" = "--help" ];
then
	echo -e "run gpu to get gpu in use.\n Source:\t https://unix.stackexchange.com/questions/16407/how-to-check-which-gpu-is-active-in-linux"
	exit 0
fi

glxinfo|egrep "OpenGL vendor|OpenGL renderer"
